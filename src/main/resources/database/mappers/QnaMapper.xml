<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iu.base.board.qna.QnaDAO">

<sql id="search">
	<where>
		<if test="kind=='title'">
			TITLE LKIE CONCAT ('%',#{search},'%')
		</if>
		<if test="kind=='contents'">
			CONTENTS LIKE CONCAT ('%',#{search},'%')
		</if>
		<if test="kind=='writer'">
			WRITER LIKE CONCAT ('%',#{search},'%')
		</if>
	
	</where>
</sql>


<insert id="setInsert" parameterType="QnaVO" useGeneratedKeys="true" keyProperty="num">
	<selectKey keyProperty="num" resultType="Long" order="BEFORE">
		SELECT LAST_INSERT_ID()
	</selectKey>
	INSERT INTO QNA (NUM, TITLE, CONTENTS, WRITER, WRITEDATE, HIT, REF, STEP, DEPTH)
	VALUES (#{num}, #{title}, #{contents}, #{writer}, now(), 0, #{num}, 0, 0)
</insert>


<insert id="setFileInsert" parameterType="BoardFileVO">
	INSERT INTO QNAFILES (NUM, FILENAME, ORINAME)
	VALUES (#{num}, #{fileName}, #{oriName})
</insert>

<select id="getFileDetail" parameterType="BoardFileVO" resultType="BoardFileVO">
	SELECT * FROM QNAFILES WHERE FILENUM = #{fileNum}
</select>

<select id="getDetail" parameterType="BoardVO" resultType="BoardVO" resultMap="getDetailResult">
	SELECT * FROM QNA q
	LEFT JOIN
	QNAFILES qf
	ON (q.NUM = qf.NUM)
	WHERE q.NUM = #{num}
</select>
<resultMap type="QnaVO" id="getDetailResult">
	<id column="NUM" property="num"/>
	<result column="TITLE" property="title" />
	<result column="CONTENTS" property="contents"/>
	<result column="WRITER" property="writer"/>
	<result column="WRITEDATE" property="writeDate" />
	<result column="HIT" property="hit"/>
	<result column="REF" property="ref"/>
	<result column="STEP" property="step"/>
	<result column="DEPTH" property="depth"/>
	<collection property="boardFileVOs" javaType="List" ofType="BoardFileVO">
		<id column="FILENUM" property="fileNum"/>
		<result column="FILENAME" property="fileName"/>
		<result column="ORINAME" property="oriName"/>
	</collection>
</resultMap>


<select id="getTotalCount" resultType="Long" parameterType="Pager">
	SELECT COUNT(NUM) FROM QNA
	<include refid="search"></include>
</select>


<select id="getList" resultType="QnaVO" parameterType="Pager">
	SELECT * FROM QNA
	<include refid="search"></include>
	ORDER BY NUM DESC
	LIMIT #{startRow}, #{perPage}
	
</select>







</mapper>